# Setup

**To fix "Cannot find module 'react'"**, install dependencies:

```bash
npm install
```

Or double-click `install.bat` in this folder.

Requires [Node.js](https://nodejs.org) (LTS) to be installed first.
 m,,nn 